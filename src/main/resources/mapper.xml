<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.wuwei.test.dao.Dao">
    
    <insert id="addStudent" parameterType="com.wuwei.test.entity.Student">
        insert into student(name,course)values(#{name},#{course})
    </insert>
    
    <select id="getAllStudent" resultType="com.wuwei.test.entity.Student">
        select * from student
    </select>
    
    <select id="getStudentById" resultType="com.wuwei.test.entity.Student">
        select * from student where id = #{id}
    </select>
    
    <update id="updateStudent" parameterType="com.wuwei.test.entity.Student">
        update student set name = #{name}, course = #{course} where id = #{id}
    </update>
    
    <update id="delStudentById" parameterType="int">
        delete from student where id = #{id}
    </update>
    
</mapper>
